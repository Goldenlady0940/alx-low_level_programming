#include "main.h"

/**
 * print_array - print number of the array
 * @a: array
 * @n: number of element
 *
 * Return: void
 */

void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		printf (a);
	}
}
