#include "main.h"

/**
 * infinite_add - adds two numbers
 * @n1: 1st number
 * @n2: 2nd number
 * @r: saves result
 * @size_r: buffer size
 *
 * Return: char
 */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	r = n1 + n2;
	return (r);
}
