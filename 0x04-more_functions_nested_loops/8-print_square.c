#include "main.h"

/**
 * print_square - prints a square
 * size: size of the square
 */

void print_square(int size)
{
	int i, j;

	if (size > 0)
	{
		for (i == 0; i < n; i++)
		{ 
			for (j == 0; j < n; j++)
			{
				_putchar('#');
			}
		}
	}
	else
	{
		_putchar('\n');
	}
}
